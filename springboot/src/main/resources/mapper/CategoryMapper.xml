<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.juliy.mapper.CategoryMapper">

    <select id="selectCategories"
            resultType="com.juliy.model.vo.CategoryVO">
        SELECT c.id,
               c.category_name,
               COUNT(a.id) AS article_count
        FROM t_category c
                 LEFT JOIN (SELECT *
                            FROM t_article
                            WHERE is_delete = 0
                              and `status` in (1, 2)) a ON c.id = a.category_id
        GROUP BY c.id
    </select>

    <select id="selectCategoriesAdmin"
            resultType="com.juliy.model.vo.CategoryAdminVO">
        SELECT
        c.id,
        c.category_name,
        COUNT(a.id) as article_count,
        c.create_time
        FROM t_category c
        LEFT JOIN t_article a ON c.id = a.category_id
        <where>
            <if test="condition.keywords != null">
                category_name like concat('%', #{condition.keywords}, '%')
            </if>
        </where>
        GROUP BY c.id
        LIMIT #{current},#{size}
    </select>
</mapper>